export class Contact {
	uid: string;
	name: string;
	jobTitle: string;
	organization: string;
	email: string;
	phone: string;
	hasSchedule: boolean; // Does this contact have meetings scheduled for them by their team? 
}
